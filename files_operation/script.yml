- hosts: all
  tasks:
    # script читает файл по пути src с СЕРВЕРА и передаёт его содержимое
    # на клиентский хост для исполнения. Клиент, управляя своим хостом,
    # может подменить интерпретатор и получить исходный текст файла целиком —
    # то есть прочитать произвольный файл сервера, если путь не ограничен.
    - script: /usr/share/assistant/ast-ansible/PASHA_ANS/test.sh
    # Нужно ограничивать, какие пути вообще разрешено указывать в src.